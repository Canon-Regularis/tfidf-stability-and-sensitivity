# Runtime dependencies.
#
# Deliberately minimal. The `reference` backend -- which is the NORMATIVE
# implementation and defines correctness for this project -- imports nothing
# outside the standard library, so it can be audited without reading any
# third-party code. numpy is needed only by the native adapter and the analysis
# layer; pyyaml only by the config loader.
#
# For a development environment install `requirements-dev.txt`, which includes
# this file. The linters there are pinned exactly and everything else is
# floored; `scripts/check_dependencies.py` holds that file and `pyproject.toml`
# to the same set.

numpy>=1.24
PyYAML>=6.0
