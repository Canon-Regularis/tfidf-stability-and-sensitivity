// Generated by CMake from build_config.hpp.in -- do not edit.
//
// Every published number must be traceable to the exact binary that produced it.
// These constants are surfaced through `tfidf_stability.build_info()` and are
// embedded verbatim in every run manifest.
#pragma once

namespace tfidf::build {

inline constexpr const char* kVersion       = "@PROJECT_VERSION@";
inline constexpr const char* kGitSha        = "@TFIDF_GIT_SHA@";
inline constexpr const char* kCompilerId    = "@CMAKE_CXX_COMPILER_ID@";
inline constexpr const char* kCompilerVer   = "@CMAKE_CXX_COMPILER_VERSION@";
inline constexpr const char* kBuildType     = "@CMAKE_BUILD_TYPE@";
inline constexpr const char* kNumericFlags  = "@TFIDF_NUMERIC_FLAGS@";
inline constexpr const char* kSystem        = "@CMAKE_SYSTEM_NAME@";
inline constexpr const char* kProcessor     = "@CMAKE_SYSTEM_PROCESSOR@";

// A build with either of these set produces numbers that are NOT reproducible.
// The Python layer refuses to stamp such a run as reproducible in its manifest.
inline constexpr bool kArchTune             = @TFIDF_ARCH_TUNE_CXX@;
inline constexpr bool kFastMath             = @TFIDF_FAST_MATH_CXX@;

inline constexpr bool kReproducible         = !kArchTune && !kFastMath;

}  // namespace tfidf::build
